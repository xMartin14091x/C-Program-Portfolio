#include <stdio.h>
#include <string.h>

int main() {
char StudentID[8] = { };
if(scanf(" %s", StudentID) == 1) {
    StudentID[2] == 49 && StudentID[3] == 54 ? printf("yes") : printf("no");
} else return 1;

return 0;
}